package cellsociety;

import javafx.scene.layout.GridPane;
import javafx.scene.shape.Rectangle;

public class fillGridPane {

    public static void fillGrid(GridPane myGrid){
        // take in two arraylists one of states and other is colors
        //need to get the states from the backend and then fill based on the states and the colors
       //myGrid.add(new Rectangle(GRID_WIDTH,GRID_WIDTH), 1, 1);
    }
}
